/**
 * Route: `/appointments/search`
 * Screen UI: `components/schedule/appointmentSearch` (appointments → schedule asymmetry).
 */
import { AppointmentSearchScreen } from "@/components/schedule/appointmentSearch/AppointmentSearchScreen";

export default function AppointmentSearchRoute() {
  return <AppointmentSearchScreen />;
}
